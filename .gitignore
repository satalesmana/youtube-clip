node_modules
.output
.nitro
.env
*.log
npm-debug.log*

# whisper.cpp ggml model files (large binaries, download per-machine)
models/*.bin

# Per-video workspaces: outputs/{videoId}/... (downloads, temp, transcripts, clips, etc.)
outputs/*
!outputs/.gitkeep

.DS_Store
.hermes
